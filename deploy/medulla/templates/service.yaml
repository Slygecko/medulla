apiVersion: v1
kind: Service
metadata:
  name: {{ include "medulla.fullname" . }}
  labels:
    {{- include "medulla.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medulla.selectorLabels" . | nindent 4 }}
