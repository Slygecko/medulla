apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medulla.fullname" . }}-test-connection"
  labels:
    {{- include "medulla.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medulla.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
